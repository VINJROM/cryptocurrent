import React from "react";
import styled, { css } from "styled-components";
import { SelectableTile } from "../Shared/Tile";

export default ({price, index}) =>{
return <SelectableTile>{}</SelectableTile>
}
